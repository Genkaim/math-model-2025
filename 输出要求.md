# 排版格式要求（基于 CUMCM 模板）

自采用 CUMCM 模板（"C:\Users\cxh20\Documents\AAACode\MathModel\优秀论文\Latex模板"）编译论文后，**字体、字号、页面版式等由模板统一控制**，不再手工设置。本文档记录模板的使用方式、写作规范与 LaTeX 适配经验。注意需要把模板文件复制到实际工作目录下的输出文件夹再修改，而非直接修改模板源文件。

实际工作目录位于当前项目目录下的“最终输出”文件夹。并把所有用到的文件放入“最终输出/支持文件”。图片则放入“最终输出/图片”，用于渲染pdf。

## 模板使用

- 文档类：`\documentclass[withoutpreface,bwprint]{commons/cumcmthesis}`（电子版；去掉 `withoutpreface` 可包含承诺书与编号页）。
- 编译：`xelatex main`（需 2~3 次，引用稳定）或 `latexmk main`。
- 项目结构：
  - `main.tex`：入口，`\input` 各 contents 文件。
  - `commons/cumcmthesis.cls`：模板文档类（已加载 longtable/float/caption/array/booktabs 等宏包）。
  - `commons/preamble.tex`：导言区（已添加计数器重置、4 级标题样式）。
  - `contents/abstract.tex`：摘要；`contents/sections/s1~s6.tex`：正文；`contents/references.tex`：参考文献。
  - `图片/`：论文图片目录（路径 `图片/图X.png`）。

## 模板已设定的格式（勿手工改）

- 页面：A4，四边距 **25mm**。该值直接写在**工作副本** `commons/cumcmthesis.cls` 的 `\geometry{top=25mm,bottom=25mm,left=25mm,right=25mm}` 中（此工作副本本就允许修改，源模板 `Latex模板` 同值）。`commons/preamble.tex` 不再对页边距做覆盖；迁移/重导模板后需复核 cls 该行是否仍为 25mm。
- 正文：12.05pt，行距 `\baselinestretch{1.38}`，首行缩进 2em。
- 字体：中文主字体宋体（SimSun，ctex 默认 Windows 字体）并开启伪粗体；西文 Times（`fonts/times`）。**标题（section/subsection/subsubsection/paragraph）为宋体加粗**——cls 的标题格式只写 `\bfseries` 而未加 `\heiti`，加粗来自主字体的 AutoFakeBold。`\heiti` 黑体仅用于"关键词"标签、目录条目、承诺书/编号页封面。
- 章节编号：section 自动汉字编号（"一、"居中）；subsection/subsubsection 数字编号；paragraph 为 4 级（已开启 `\setcounter{secnumdepth}{4}`）。
- 图表标题：`captionsetup` 宋体小四加粗；图注在下、表题在上。
- 表格行距：`tabular` 环境内 `\arraystretch{1.38}`（`longtable` 不受此影响）。

## 写作规范

### 标题
- **去掉所有手写编号**，由模板自动生成：写 `\section{问题重述}` 而非 `\section{一、问题重述}`、`\subsection{问题背景}` 而非 `\subsection{1.1 问题背景}`。
- 按编号段数映射层级：
  - 2 段编号（如 5.1、1.1）→ `\subsection`
  - 3 段编号（如 5.1.1、5.4.6）→ `\subsubsection`
  - 4 段编号（如 5.2.2.1、5.4.6.1）→ `\paragraph`（模板中 paragraph 已改为独立标题样式）

### 公式
- 行间公式独占一行居中；行内公式用行内模式。
- 公式编号右对齐圆括号 (1)(2)(3)…，全文连续，用 `\tag{n}` 放在公式块末尾（`\[...\]` 不自动编号）。
- 多行公式用 `aligned` 对齐；分段函数/约束组用 `cases`，编号放整个环境右端。
- 关键推导给行间公式，琐碎过程用行内公式。

### 表格
- 跨页表格用 `longtable`；**表题用 `\caption{标题}` 自动编号**（模板显示"表 1 标题"），勿用 `\caption*{表N}` 手动编号。
- 表题位于表格上方；续页表头写 `\multicolumn{n}{c}{表N（续）}`，其中的表号须与自动编号一致。
- **表头行末尾加 `\\[-0.7\baselineskip]`**：抵消 longtable 表头块与数据块之间的固有空隙。
- **`\endfoot` `\endlastfoot` 紧跟在 `\endhead` 之后（数据行之前）**，且保持为空。
- **表格底部横线放在 `\endlastfoot` 之前**（lastfoot 块内），不要在最后一行数据后再 `\hline`——否则该横线会被延迟输出到下一页顶部。
- 宽表内容可用 `{\zihao{6}...}` 缩小字号；列宽用 `>{\centering\arraybackslash}m{...}` 控制。

### 图片
- `\begin{figure}[H]` 强制内联（防浮动错位），或显式 `[htbp]`，**不得省略定位参数**。
- 图注用 `\caption*{图N 标题}` 或 `\caption{标题}`，位于图片下方。
- 宽度按比例：半栏 0.45、中图 0.65、通栏 0.85 倍文本宽，`keepaspectratio`。

### 参考文献
- 用 `\begin{thebibliography}{9}` + `\bibitem[N]{key}` 格式（见 `contents/references.tex`）。

## 经验与注意事项

1. **SimSun 无真粗体 → 正文/标题加粗必须挂在"主字体"上（关键坑）**：cls 只依赖 ctex 默认主字体（SimSun），未给主字体开 AutoFakeBold。若只在**命名家族**开伪粗体（如旧写法 `\setCJKfamilyfont{zhsong}{SimSun}[AutoFakeBold={4}]`），`\textbf{}/\bfseries` 仍作用于主字体，SimSun 无真粗体时 xeCJK 会**回退成黑体**——即"粗体变黑体"症状。正确做法（见 `commons/preamble.tex`）：
   ```latex
   \setCJKmainfont[AutoFakeBold={4}]{SimSun}   % 主字体开伪粗体，\bfseries/\textbf 才是加粗宋体
   \setCJKfamilyfont{song}{SimSun}[AutoFakeBold={4}]
   \setCJKfamilyfont{zhsong}{SimSun}[AutoFakeBold={4}]
   \setCJKmathfont{SimSun}
   ```
   验证（PyMuPDF）：章节标题 basefont=SimSun、flags=4（serif、不带 bold 位）→ 加粗由 AutoFakeBold 合成描边产生；无任何 CJK span 落到 SimHei 黑体。
2. **汉字编号不能写 `[一-九]` 范围**：汉字编码无序（"五" U+4E94 大于 "九" U+4E5D），正则需用显式字符集 `[一二三四五六七八九]`。
3. **公式后紧跟文字（无空行）不会自动缩进**（`\[...\]` 后），需在段首手动加 `\indent`。
4. **所有 `\begin{figure}` 必须显式带定位参数**，漏写默认 `[tbp]` 会导致图片浮动到远处（曾致图 11 错位）。
5. **longtable 最后一条 `\hline` 在 `\endlongtable}` 前会延迟到下一页**（TeX Live 2026 行为），底部线应放入 `\endlastfoot` 块（曾致表 5 后图片上方出现多余横线）。
6. **模板计数器**：preamble.tex 已处理 subsection/subsubsection/paragraph 重置（否则编号全局累计、5.4.6 会错显为 5.19）、`secnumdepth=4`、paragraph 独立标题样式。
7. **表格编号曾整体错位**（正文表 2-8 标成 1-7），正文表格应从"符号说明=表1"起连续编号。
8. **验证排版**：用 PyMuPDF `page.get_text('words')` 取文字的 x/y 坐标（可判缩进、行距、编号），比渲染看图可靠。
9. **公式内中文不显示（关键坑）**：xeCJK 默认**不在数学模式**（`$...$` / `\[...\]`）渲染汉字。正文里如 `$W_重$`、`$W_l$`、`$x_r(x_c-d)$`、`$D_0$` 这样带中文下标/上标的公式，汉字会整段丢失或报错。修复（见 `commons/preamble.tex`）：
   ```latex
   \xeCJKsetup{CJKmath=true}   % 允许在数学模式里直接写汉字
   \setCJKmathfont{SimSun}     % 公式中文用宋体（主字体已开 AutoFakeBold，公式内同样加粗）
   ```
   验证：编译后全文 `get_text()` 能搜到这些汉字（如"重""左""右""坡度""水深"），即说明已正常渲染；若搜不到，多半是漏了 `CJKmath=true`。
